FROM node:20-alpine

WORKDIR /app

# Instalar dependencias primero para aprovechar la cache de Docker
COPY package.json package-lock.json* ./
RUN npm install

# El código se monta como volumen en docker-compose.yml para hot-reload
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
